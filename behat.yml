default:
  suites:
    default:
      paths:    [ %paths.base%/features/default ]
      contexts:
        - RPGBehat\FeatureContext

  extensions:
    Behat\Symfony2Extension: ~
    Behat\MinkExtension:
          base_url:  'nginx'
          sessions:
            default:
              goutte: ~

